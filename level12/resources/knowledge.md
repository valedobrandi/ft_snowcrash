The Exploit
The Perl script passes user input directly into a shell backtick command with no protection against shell metacharacters. By injecting $() command substitution, we break out of the intended egrep command and execute arbitrary code — inheriting the SUID privileges of flag12.
The wildcard /*/CRACK bypasses both problems simultaneously — it survives the uppercase filter AND resolves correctly inside Perl's privileged shell execution.