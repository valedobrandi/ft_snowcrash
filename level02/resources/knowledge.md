The Exploit
Credentials transmitted over the network in plain text with no encryption — anyone with access to the network traffic can trivially recover them using standard packet analysis tools.
Passwords sent without encryption (no SSH, no TLS) are fully exposed in packet captures