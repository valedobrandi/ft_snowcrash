ltrace ./level09 
strace ./level09 

The Trick: The binary calls ptrace(PTRACE_TRACEME) itself.

The Result: Since ltrace is already tracing the binary, 
the binary's own request to trace itself fails (returns -1).

Input: aaaaa (ASCII 97, 97, 97, 97, 97)
Index 0: a + 0 = a
Index 1: a + 1 = b
Index 2: a + 2 = c
Index 3: a + 3 = d
Index 4: a + 4 = e
Output: abcde

s5cAJpM8ev6XHw998pRWG728z